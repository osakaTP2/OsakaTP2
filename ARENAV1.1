--==================================================
-- UI ONLY
--==================================================
local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")

-- KAVO
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("OsakaTP2 | 🍌","DarkTheme")

-- DIAMOND
local DiamondTab = Window:NewTab("DIAMOND")
local DiamondSection = DiamondTab:NewSection("Diamond Automation")
DiamondSection:NewToggle("Auto Pull Diamond","Pull nearby diamonds automatically",function(val)
    _G.AUTO_PULL = val
end)

-- FIRE
local FireTab = Window:NewTab("FIRE")
local FireSection = FireTab:NewSection("Fire Sword Skills")
FireSection:NewToggle("Auto Fire Q","Use Q automatically",function(val)
    _G.AUTO_FIRE2 = val
end)
FireSection:NewToggle("Auto Fire X","Use X automatically",function(val)
    _G.AUTO_FIRE3 = val
end)

-- CHAOS
local ChaosTab = Window:NewTab("CHAOS")
local ChaosSection = ChaosTab:NewSection("Chaos Skills")
ChaosSection:NewToggle("Auto Chaos Attack","Attack nearby players automatically",function(val)
    _G.AUTO_CHAOS = val
end)

-- LAVA
local LavaTab = Window:NewTab("LAVA")
local LavaSection = LavaTab:NewSection("Lava Skills")
LavaSection:NewToggle("Auto Lava","Activate Lava Skill automatically",function(val)
    _G.AUTO_LAVA = val
end)

-- LIGHT
local LightTab = Window:NewTab("LIGHT")
local LightSection = LightTab:NewSection("Light Skills")
LightSection:NewToggle("Auto Light Q","Teleport + Q multiple times",function(val)
    _G.AUTO_LIGHT_Q = val
end)
LightSection:NewToggle("Auto Light Orb","Fire Light Orb automatically",function(val)
    _G.AUTO_LIGHT_ORB = val
end)

-- FLOATING BUTTON
local MenuGui = Instance.new("ScreenGui")
MenuGui.Name = "FloatingMenu"
MenuGui.ResetOnSpawn = false
MenuGui.Parent = PlayerGui

local Button = Instance.new("TextButton")
Button.Size = UDim2.fromOffset(50,50)
Button.Position = UDim2.fromScale(0.12,0.8)
Button.Text = "🍌"
Button.TextSize = 28
Button.Font = Enum.Font.GothamBold
Button.BackgroundColor3 = Color3.fromRGB(25,25,25)
Button.TextColor3 = Color3.fromRGB(255,255,255)
Button.ZIndex = 999
Button.Parent = MenuGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(1,0)
corner.Parent = Button

local stroke = Instance.new("UIStroke")
stroke.Thickness = 2
stroke.Color = Color3.fromRGB(80,80,80)
stroke.Parent = Button

Button.MouseButton1Click:Connect(function() Library:ToggleUI() end)

-- Drag
local dragging = false
local dragStart
local startPos
local function update(input)
    local delta = input.Position - dragStart
    Button.Position = UDim2.new(
        startPos.X.Scale, startPos.X.Offset + delta.X,
        startPos.Y.Scale, startPos.Y.Offset + delta.Y
    )
end

Button.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = Button.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

Button.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        update(input)
    end
end)
