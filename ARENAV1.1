-- ARENAV1.1 (UI ONLY)

if not game:IsLoaded() then game.Loaded:Wait() end

-- FLAGS
_G.AUTO_PULL = false
_G.AUTO_FIRE_Q = false
_G.AUTO_FIRE_X = false
_G.AUTO_CHAOS = false
_G.AUTO_LAVA = false
_G.AUTO_LIGHT_Q = false
_G.AUTO_LIGHT_ORB = false

-- KAVO
local Library = loadstring(game:HttpGet(
	"https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"
))()

local Window = Library.CreateLib("OsakaTP2 | 🍌","DarkTheme")

-- DIAMOND
local T1 = Window:NewTab("DIAMOND")
T1:NewSection("Diamond")
	:NewToggle("Auto Pull","",function(v)
		_G.AUTO_PULL = v
	end)

-- FIRE
local T2 = Window:NewTab("FIRE")
T2:NewSection("Fire Sword")
	:NewToggle("Auto Fire Q","",function(v)
		_G.AUTO_FIRE_Q = v
	end)
	:NewToggle("Auto Fire X","",function(v)
		_G.AUTO_FIRE_X = v
	end)

-- CHAOS
local T3 = Window:NewTab("CHAOS")
T3:NewSection("Chaos")
	:NewToggle("Auto Chaos","",function(v)
		_G.AUTO_CHAOS = v
	end)

-- LAVA
local T4 = Window:NewTab("LAVA")
T4:NewSection("Lava")
	:NewToggle("Auto Lava","",function(v)
		_G.AUTO_LAVA = v
	end)

-- LIGHT
local T5 = Window:NewTab("LIGHT")
T5:NewSection("Light")
	:NewToggle("Auto Light Q","",function(v)
		_G.AUTO_LIGHT_Q = v
	end)
	:NewToggle("Auto Light Orb","",function(v)
		_G.AUTO_LIGHT_ORB = v
	end)
